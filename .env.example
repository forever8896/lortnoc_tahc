# Copy to .env and fill in. .env is gitignored — never commit the real key.
# Venice AI API key, used by the venice-studio skill for asset generation.
# Kilian will share the real key privately.
VENICE_API_KEY=
